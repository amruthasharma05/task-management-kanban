import {useState} from 'react';import {Task,Priority} from '../types';export function TaskModal({task,onSave,onClose}:{task?:Task;onSave:(x:any)=>void;onClose:()=>void}){const [x,setX]=useState<any>(task||{title:'',description:'',priority:'MEDIUM',status:'TODO',assignedTo:null,dueDate:null});const set=(k:string,v:any)=>setX({...x,[k]:v});return <div className="modal-backdrop"><form className="modal" onSubmit={e=>{e.preventDefault();onSave({...x,dueDate:x.dueDate?new Date(x.dueDate).toISOString():null})}}><h2>{task?'Edit task':'Create task'}</h2><input required placeholder="Task title" value={x.title} onChange={e=>set('title',e.target.value)}/><textarea placeholder="Description" value={x.description||''} onChange={e=>set('description',e.target.value)}/><div className="form-row"><select value={x.priority} onChange={e=>set('priority',e.target.value)}><option value="HIGH">High</option><option value="MEDIUM">Medium</option><option value="LOW">Low</option></select><input type="date" value={x.dueDate?x.dueDate.slice(0,10):''} onChange={e=>set('dueDate',e.target.value)}/></div><div className="actions"><button type="button" onClick={onClose}>Cancel</button><button className="primary">Save task</button></div></form></div>}
