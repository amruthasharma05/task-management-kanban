const base='/api'; async function request<T>(path:string,options:RequestInit={}):Promise<T>{const r=await fetch(base+path,{headers:{'Content-Type':'application/json',...options.headers},...options}); if(!r.ok){const x=await r.json().catch(()=>({}));throw new Error(x.error||'Request failed')} return r.status===204?undefined as T:r.json()}; export const api={projects:()=>request<any[]>('/projects'),project:(id:string)=>request<any>('/projects/'+id),tasks:(projectId:string)=>request<any[]>('/tasks?projectId='+projectId),users:()=>request<any[]>('/users'),createTask:(x:any)=>request<any>('/tasks',{method:'POST',body:JSON.stringify(x)}),updateTask:(id:string,x:any)=>request<any>('/tasks/'+id,{method:'PUT',body:JSON.stringify(x)}),moveTask:(id:string,status:string)=>request<any>('/tasks/'+id+'/status',{method:'PATCH',body:JSON.stringify({status})}),deleteTask:(id:string)=>request<void>('/tasks/'+id,{method:'DELETE'}),addMember:(id:string,userId:string)=>request<any>('/projects/'+id+'/users',{method:'POST',body:JSON.stringify({userId})})};
